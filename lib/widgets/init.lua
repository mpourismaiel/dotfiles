local taglist = require("lib.widgets.taglist")
local tasklist = require("lib.widgets.tasklist")
local layoutbox = require("lib.widgets.layoutbox")
local keyboardlayout = require("lib.widgets.keyboardlayout")
local bar = require("lib.widgets.bar")

return {
  taglist = taglist,
  tasklist = tasklist,
  layoutbox = layoutbox,
  keyboardlayout = keyboardlayout,
  bar = bar
}
