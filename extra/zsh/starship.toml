format = """
(bg:#DA627D fg:#9A348E)\
${custom.project_pwd}\
$git_branch\
$git_commit\
$git_status\
$fill\
$cmd_duration\
$docker_context\
$deno\
$elixir\
$elm\
$erlang\
$golang\
$java\
$kotlin\
$gradle\
$lua\
$nodejs\
$php\
$python\
$conda\
$memory_usage\
$line_break$character"""

# Disable the blank line at the start of the prompt
add_newline = true

[custom.project_pwd]
when = "true"
command = "project_pwd.zsh"
format = "[$output](italic bg:#424868 fg:#A8ABC0)"

[directory]
style = "bg:#313348 fg:#A8ABC0"
truncate_to_repo = true
truncation_length = 99
truncation_symbol = ""
read_only = " ◈ "
use_os_path_sep = true
format = '[ $path ]($style)[$read_only]($read_only_style)'

[git_branch]
symbol = ""
style = "bg:#0e271d"
format = '[ $symbol $branch ]($style)'

[git_commit]
style = "bg:#091a13"
format = '[ 󰕱 $hash$tag ]($style)'
only_detached = false

[git_status]
style = "bg:#123225"
conflicted = '🏳 '
ahead = '⇡${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
behind = '⇣${count}'
up_to_date = '✓ '
untracked = ' '
stashed = ' '
modified = '${count} '
staged = '[++\($count\)](green)'
renamed = ' '
deleted = '🗑 '
format = '[ $conflicted$ahead$diverged$behind$up_to_date$untracked$stashed$modified$staged$renamed$deleted]($style)'

[fill]
symbol = ' '

[env_var.VIMSHELL]
format = "[$env_value]($style)"
style = 'green italic'

[cmd_duration]
format = "[◄ $duration ](italic white)"

[deno]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
version_format = "${raw}"
style = "bold bg:#2ec4b6"

[lua]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
version_format = "${raw}"
style = "bold bg:#2b8fb9"

[nodejs]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
version_format = "${raw}"
detect_files = ["package-lock.json", "yarn.lock"]
detect_folders = ["node_modules"]
detect_extensions = []
style = "bold bg:#3C873A"

[python]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
version_format = "${raw}"
style = "bold bg:#3776AB"

[conda]
symbol = "◯ "
format = "[ $symbol$environment ]($style)"
style = "bold bg:#2aa198"

[docker_context]
symbol = "◧ "
format = "[ $symbol$context ]($style)"
style = "bold bg:#384d54"

[elixir]
symbol = " "
format = "[ ${symbol}${version} OTP $otp_version ]($style)"
style = "bold bg:#6e4a7e"

[elm]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#60B5CC"

[golang]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#00ADD8"

[java]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#f89820"

[kotlin]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#7f52ff"

[gradle]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#023e8a"

[erlang]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#B83998"

[php]
symbol = " "
format = "[ ${symbol}${version} ]($style)"
style = "bold bg:#787CB5"

[memory_usage]
symbol = "▪▫▪ "
format = " mem [${ram}( ${swap})]($style)"

[line_break]
disabled = false

[character]
disabled = false
success_symbol = '[  ](bg:#444A68)'
error_symbol = '[  ](bg:color_red)'
