// generated files
module/launcher/list.json
extra/zsh/extra_config.zsh
extra/bin/notes-debug

# config directory at the root of the project, if it's nested then it should not be ignored
config/

# generated data
data/

# log files
logs/log-*

# vim created this, someone explain it please
.luarc.json

node_modules
*.log
*.css.map

# Python bytecode caches (auto-generated, interpreter/tool-version specific)
__pycache__/
*.pyc

# tangle-release.sh output (built configs are attached to GitHub Releases, not tracked)
dist/

ags/config.json
doom/private.el
# Clutch DB connections — contain credentials. Never commit either form:
# the plaintext, or the encrypted .gpg (encrypted-at-rest in a public repo
# is still an offline-crackable secret). Both live only on the local machine.
doom/connections.json
doom/connections.json.gpg
